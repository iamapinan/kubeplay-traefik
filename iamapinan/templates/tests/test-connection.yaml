apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "iamapinan.fullname" . }}-test-connection"
  labels:
    {{- include "iamapinan.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "iamapinan.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
